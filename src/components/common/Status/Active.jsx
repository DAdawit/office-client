import React from "react";

const Active = ({ text }) => {
  return (
    <h4 className="text-success-900 bg-success-50 rounded-[10px] px-2 py-1 border-0">
      {text}
    </h4>
  );
};

export default Active;
